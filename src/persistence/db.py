"""Conexión y esquema de SQLite (vía `sqlite3` de la stdlib, sin ORM).

TODO (Fase 2): crear la conexión y las tablas `trades` y `daily_metrics`.
La ruta de la base viene de settings.DATABASE_PATH.
"""
